class Sun extends GameObject {
	update() {
		
		constructor(x, y) {
			super(sunImage, x, y);
		}
	this.x += 2;
	if (this.x > width +
		this.width / 2){
		this.x = -sthis.width / 2;
		

this.y +=random(-1, 1);
}
